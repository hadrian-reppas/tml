#include <assert.h>
#include <stdint.h>

#include <stdio.h>

void init_tape(uint16_t* tape, size_t len) {
    printf("Hello from C!\n");
    assert(0);
}

void run(uint8_t* bytes, size_t max_moves) {
    assert(0);
}

uint32_t get_final_address() {
    assert(0);
    return 0;
}

uint16_t* get_tape() {
    assert(0);
    return NULL;
}

size_t get_tape_len() {
    assert(0);
    return 0;
}

size_t get_tape_head_position() {
    assert(0);
    return 0;
}

void free_tape() {
    assert(0);
}

size_t get_move_count() {
    assert(0);
    return 0;
}
